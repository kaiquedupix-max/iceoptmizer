(()=>{const snow=document.querySelector('.snow');if(snow){for(let i=0;i<46;i++){const f=document.createElement('span');f.className='flake';f.textContent=i%4?'•':'❄';f.style.left=Math.random()*100+'vw';f.style.fontSize=5+Math.random()*13+'px';f.style.animationDuration=8+Math.random()*13+'s';f.style.animationDelay=-Math.random()*18+'s';f.style.setProperty('--drift',(-45+Math.random()*90)+'px');snow.appendChild(f)}}const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(x=>observer.observe(x));const msg=document.querySelector('#checkout-message');document.querySelectorAll('.buy').forEach(button=>button.addEventListener('click',async()=>{const old=button.textContent;document.querySelectorAll('.buy').forEach(x=>x.disabled=true);button.textContent='Abrindo pagamento seguro...';if(msg)msg.textContent='Você será direcionado ao checkout seguro do Stripe.';try{const response=await fetch('/api/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({product:button.dataset.product})}),data=await response.json();if(!response.ok)throw Error(data.message||'Não foi possível iniciar o pagamento.');location.href=data.url}catch(e){if(msg)msg.textContent=e.message;document.querySelectorAll('.buy').forEach(x=>x.disabled=false);button.textContent=old}}))})();
